{
  "name": "@atlas/mcp-server",
  "version": "0.1.0",
  "description": "Atlas Local Hybrid Search MCP Server — privacy-first knowledge engine",
  "type": "module",
  "main": "src/mcp-server.ts",
  "scripts": {
    "dev": "bun run --watch src/mcp-server.ts",
    "start": "bun run src/mcp-server.ts",
    "index": "bun run src/scripts/index-worker.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "better-sqlite3": "^11.7.0",
    "sqlite-vec": "^0.1.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "typescript": "^5.6.0"
  }
}
